// Package gatedimport is reachable from sub only under the "gated"
// build tag: the linked-set derivation must see the selection's tags.
package gatedimport

// V anchors the gated reference.
var V = 1
