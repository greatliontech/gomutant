// Package indirectprop exercises rapid solely through a helper: its
// own test files never import pgregory.net/rapid.
package indirectprop

// Double is the subject.
func Double(x int) int { return x * 2 }
