// Package prop is the clean property-oracle fixture: its test binary
// reaches nothing beyond the rapid stub and the harness, so a
// rapid-oracle finding's observation proof records Observable and the
// record serves plainly cached on an unchanged tree. The lib fixture
// deliberately stays a kitchen sink (unsafe grammar shapes, ambient
// tests) whose proofs refuse - serve behavior there is drift
// re-measure by design.
package prop

func Add(a, b int) int { return a + b }
