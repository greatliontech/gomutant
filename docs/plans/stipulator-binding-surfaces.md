# Stipulator binding surfaces

- [x] 1. Replace the retired Stipulator target-document contract with strict binding-surface validation, Go compatibility, explicit oracle, label, run-policy, and finding-correlation semantics.
- [ ] 2. After Stipulator publishes its separately versioned Go wire module and authoritative fixtures, depend on the module, copy the fixtures, and replace the legacy adapter with all-Go compatibility and target mapping over module-validated reports.
- [ ] 3. Verify CLI and MCP target loading, explicit-empty behavior, label refresh on reused findings, remove the legacy parser and fixtures, and delete this plan.
