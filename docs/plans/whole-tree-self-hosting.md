# Whole-tree self-hosting

- [ ] 1. In Gofresh, contract readable-file value independently from metadata-only observation and define caller-proven producer-owned ephemeral roots without weakening external runtime-input identities.
- [ ] 2. In Gofresh, remove mtime from regular-file read digests while retaining content, existence/type, executable-mode, symlink, and metadata-only negative controls plus merge algebra coverage.
- [ ] 3. In Gofresh, add environment-aware testlog finalization that excludes only paths beneath caller-proven producer-owned roots, preserving outside identities, relative-path resolution, and canonical version-1 manifests.
- [ ] 4. In Gofresh, expose context-aware batched view construction and checking with per-subject fingerprints, cancellation, and equivalence to singleton analysis.
- [ ] 5. Run Gofresh adversarial, mutation, race, and cross-platform verification; publish the dependency release consumed by gomutant.
- [ ] 6. In gomutant, contract producer-owned temporary execution and deterministic preparation progress, including ordering, cancellation, and CLI/MCP representation.
- [ ] 7. Update Gofresh and run every oracle process under one stable, context-locked, initially empty per-tree temporary root passed to policy-aware observation finalization.
- [ ] 8. Prove package-derived `t.TempDir` oracles and the explicit self-host matrix produce equivalent admissible findings under serial and concurrent cold/warm-cache runs while real external-input changes still refuse or stale.
- [ ] 9. Emit deterministic preparation events for loading, target/oracle resolution, freshness, mutant generation, and baseline groups before ordered run decisions, with CLI and MCP parity.
- [ ] 10. Replace per-subject singleton freshness construction with context-aware module-batched views and one post-execution validation boundary per module.
- [ ] 11. Memoize run-local package-derived oracles, oracle validation, package contexts, rapid classification, and repeated runtime-manifest checks without changing target or cache semantics.
- [ ] 12. Skip import processing for mutation families that cannot orphan imports, retain a conservative fallback for removal-capable mutations, and deduplicate effective source again after pruning.
- [ ] 13. Thread cancellation through package loading, freshness construction/checking, mutant enumeration/rendering, and every remaining expensive preparation boundary without publishing partial findings.
- [ ] 14. Complete uncapped whole-tree self-hosting, disposition every survivor, prove repeat reuse and findings hygiene, update standalone workflow documentation, delete the resolved issue, and delete this plan.
