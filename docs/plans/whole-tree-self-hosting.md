# Whole-tree self-hosting

- [x] 1. Separate fresh mutation outcomes from reusable evidence: runtime-observation disagreement or movement records canonical unverifiable evidence while baseline result instability still refuses measurement, with end-to-end regression coverage.
- [x] 2. Prove package-derived `t.TempDir` oracles and the explicit self-host matrix complete under serial and concurrent cold/warm-cache runs, with unstable evidence remeasured rather than cached.
- [x] 3. Emit deterministic preparation events for loading, target/oracle resolution, freshness, mutant generation, and baseline groups before ordered run decisions, with CLI and MCP parity.
- [x] 4. Replace per-subject singleton freshness construction with context-aware module-batched views and one post-execution validation boundary per module.
- [x] 5. Memoize run-local package-derived oracles, oracle validation, package contexts, rapid classification, and repeated runtime-manifest checks without changing target or cache semantics.
- [x] 6. Skip import processing for mutation families that cannot orphan imports, retain a conservative fallback for removal-capable mutations, and deduplicate effective source again after pruning.
- [x] 7. Thread cancellation through package loading, freshness construction/checking, mutant enumeration/rendering, and every remaining expensive preparation boundary without publishing partial findings.
- [x] 8. Specify the comprehensive first-order Go mutation basis, including exact operator mappings, applicability and exclusions, candidate ordering, budget semantics, discard conservation, result pins, and the prerequisite relationship to final whole-tree self-hosting.
- [x] 9. Introduce candidate-level generation, total candidate counts, selected-candidate decisions, and exact generated-source assertions, conserving no-op, duplicate, and compile discards through deterministic budgets and end-to-end per-operator findings while updating the active-basis clause with the new identifier and exact membership.
- [x] 10. Complete equality, relational-boundary, relational-negation, and logical mutations at every specified type-compatible site, extending the fixture grammar, exact-source assertions, stale-basis rejection, and outcome accounting while updating the active-basis clause with the new identifier and exact membership.
- [x] 11. Complete condition forcing and negation, boolean-operand forcing, break/continue legality, and range suppression with the same grammar, source, stale-pin, and accounting coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 12. Complete arithmetic and remainder binary mutations across specified numeric domains, constants, defined types, and compile-discard cases with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 13. Complete bitwise, bit-clear, and shift binary mutations across specified integer domains, constants, defined types, and compile-discard cases with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 14. Complete unary, compound-assignment, compound-store, and increment/decrement mutations corresponding to the landed expression domains with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 15. Complete integer, rune, floating-point, imaginary, boolean, and string literal mutations across specified lexical, shadowing, overflow, and compile-discard cases with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 16. Complete declared-result-slot return substitutions across specified built-in, defined, nil-capable, interface, multi-result, and no-op/discard cases with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [x] 17. Complete statement deletion, assignment-store dropping, and selected block emptying across blocks, cases, communication bodies, initializers, and post statements, preserving specified evaluation and import semantics with the same end-to-end coverage while updating the active-basis clause with the new identifier and exact membership.
- [ ] 18. Audit the finalized basis against the Go syntax inventory and operator-site fixture grammar, inspect representative generated sources, reconcile every operator row, disposition bounded self-host survivors, and prove serial, concurrent, and repeat findings behavior without changing the basis.
- [ ] 19. Complete the uncapped overnight whole-tree self-hosting run under the finalized basis, disposition every survivor, prove repeat behavior and findings hygiene, update standalone workflow documentation, promote remaining issue rationale, delete the resolved issue and index entry, and delete this plan.
