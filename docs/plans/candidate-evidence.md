# Candidate-granular runtime evidence

- [x] 1. Amend the execution and results contracts: runtime-input evidence attaches per candidate — completed processes' deterministic union serves their candidates, each process that cannot prove its log complete marks its own candidate's evidence unverifiable — while measurement pins (source, configuration, oracle identity, operator version, budgets) keep whole-target granularity; a finding whose only unverifiable evidence is candidate-local is served with exactly those candidates re-executed under a passing current baseline, and a false kill retained past a moved runtime input remains the forbidden flattering direction.
- [x] 2. Implement per-candidate evidence in the finding model and persistence, with reuse serving completed-evidence candidates and re-executing unverifiable-evidence candidates under the same probe, splice conserving INV-RESULT-CANDIDATE-CONSERVATION.
- [ ] 3. Share one gofresh engine and observed-batch capture across a run's targets, replacing the per-target view construction.
- [ ] 4. Cache the loaded engine tree across MCP tool calls with tree-change invalidation.
- [ ] 5. Commit findings incrementally per finished target under the existing document lock so an interrupted run is resumable and bounds lost work.
- [ ] 6. Report MCP progress from the existing preparation and decision events plus gofresh analysis-phase progress, and default the MCP run timeout below typical client deadlines.
- [ ] 7. Document the double-execution semantics of observed runs in the run and ephemeral tool descriptions.
- [ ] 8. Pin the shared symbol grammar at the Stipulator adapter seam with a method-symbol fixture captured from a real stipulator targets export.
- [ ] 9. Promote the surviving findings of the gomutant–gofresh boundary audit into issue docs or specs and delete the audit file.
- [ ] 10. Close out: re-measure a representative MCP run against the pre-plan baseline, release, and delete this plan.
