# Issue docs — deferred follow-ups

Tracked deferrals carrying a `Lands:` trigger. On resolution, the
load-bearing rationale is promoted inline to the spec / a test, and the doc
is deleted (git holds history).

| slug | summary | Lands |
|------|---------|-------|
| [mcp-liveness-cancellation-witness](mcp-liveness-cancellation-witness.md) | keepalive config pinned but the ping-failure cancellation propagation has no witness over the SDK transport seam | transport-seam fault injection lands in the mcpserver harness |
| [windows-process-fact-arms-unexecuted](windows-process-fact-arms-unexecuted.md) | windows process facts (killed flag, job kills, timeout attribution) are compile-checked only; two attribution defects survived to review state walks | when a windows runner enters the CI matrix |
| [run-scoped-services-through-options](run-scoped-services-through-options.md) | groupBudget/probeGate/scheduleStore ride exported Options as unexported fields; collapse into one explicitly threaded runServices value | with train chunk 136 |
| [ephemeral-attestation-lifecycle](ephemeral-attestation-lifecycle.md) | attestation rows have no prune/retarget lifecycle; commit+dirty stamp is the only staleness signal | with the next change to prune or retarget record surfaces |
| [semantic-closure-in-the-carry-gate](semantic-closure-in-the-carry-gate.md) | a comment-insensitive closure identity would dominate both poles of the carry gate | train chunk 130 (after 129 ships the gofresh identity) |
| [own-face-gate-suite-decomposition](own-face-gate-suite-decomposition.md) | chunk-137's narrowing measured weak on gomutant's e2e-heavy face (covering ≈ suite); own-face gates stay on ephemeral probes; suite decomposition is the fork's unchartered (a) half | user decision |
| [probe-phase-cost-invisible](probe-phase-cost-invisible.md) | a window's coverage-probe phase (~2h measured) precedes the estimate event and ticks — frozen progress, no projection; probe cost is modelable from banked durations | with train chunk 136, or the next change to the window cost model |
| [suite-shared-fixture-bracket-flake](suite-shared-fixture-bracket-flake.md) | one full-suite parallel run moved the shared fixturemod/lib observation bracket (writer unidentified; refusal-attribution instrumentation landed in gofresh) | when the instrumented refusal names the writer (next occurrence under a gofresh bump past v0.92.0) |
| [symbol-grammar-package-set-resolution](symbol-grammar-package-set-resolution.md) | symbolPackage's dotted-path guess has one verdict-bearing edge (dotted sibling dirs grant process-attestability across packages); resolve against the loaded package set | when a dotted non-vN package element enters a consumed corpus, or with the next packageProcessAttestable change |
| [rescore-mechanism-unification](rescore-mechanism-unification.md) | the serial confirmation and the narrowed-survivor audit duplicate the replace-wholesale rescore (gate posture diverged once already); collapse to one mechanism | with train chunk 136 (the fired change-trigger was dispositioned REDEFER during chunk 137 — see the doc) |
| [delta-line-survivor-view](delta-line-survivor-view.md) | a changed-ref campaign reports whole-symbol survivors; the delta's added-line survivors need hand reconstruction from the JSON dump and the diff | cross-tool train chunk 139 |
| [ephemeral-test-pkg-shorthand](ephemeral-test-pkg-shorthand.md) | `ephemeral --test-pkg .` refuses the shorthand a module-local caller reaches for | with train chunk 138 |
| [bracket-path-module-relative-in-workspace](bracket-path-module-relative-in-workspace.md) | a relative bracket path joins onto the target module, so a nested workspace module never matches a root-module surface; absolute paths force machine-local records | cross-tool train chunk 138 |
| [ephemeral-deletion-probes-strand-imports](ephemeral-deletion-probes-strand-imports.md) | a deletion probe strands its error path's imports ("fmt imported and not used"), forcing neutering instead of honest deletion; imports-fix or tolerate for ephemeral | cross-tool train chunk 140 |
| [ephemeral-batch-wrapper-undiscoverable](ephemeral-batch-wrapper-undiscoverable.md) | the CLI batch form needs a `{"edits": [...]}` wrapper named in neither the guidance nor the refusal | cross-tool train chunk 140 |
| [ephemeral-blind-spots-stated-and-refused](ephemeral-blind-spots-stated-and-refused.md) | source-reading, go-list, and test-file oracles never see the mutant tree; the probe reads "survived" with an advisory instead of refusing; guidance names none of the three | cross-tool train chunk 140 |
| [ephemeral-compiler-crash-retry](ephemeral-compiler-crash-retry.md) | a transient Go compiler SIGSEGV under a probe reads as a broken mutant; retry once or mark it | cross-tool train chunk 140 |
| [ephemeral-deletion-probes-strand-imports](ephemeral-deletion-probes-strand-imports.md) | a deletion probe strands its error path's imports ("fmt imported and not used"), forcing neutering instead of honest deletion; imports-fix or tolerate for ephemeral | user decision (bldc consumer report 2026-09-02) |
| [ephemeral-batch-wrapper-undiscoverable](ephemeral-batch-wrapper-undiscoverable.md) | the CLI batch form needs a `{"edits": [...]}` wrapper named in neither the guidance nor the refusal | user decision (bldc consumer report 2026-09-02) |
| [ephemeral-blind-spots-stated-and-refused](ephemeral-blind-spots-stated-and-refused.md) | source-reading, go-list, and test-file oracles never see the mutant tree; the probe reads "survived" with an advisory instead of refusing; guidance names none of the three | user decision (bldc consumer report 2026-09-02) |
| [ephemeral-compiler-crash-retry](ephemeral-compiler-crash-retry.md) | a transient Go compiler SIGSEGV under a probe reads as a broken mutant; retry once or mark it | user decision (bldc consumer report 2026-09-02) |
| [bracket-path-lexical-match](bracket-path-lexical-match.md) | a bracket path matches the observed open's un-cleaned spelling; the clean absolute path of the same file does not | user decision |
| [never-executed-exemption-unaudited](never-executed-exemption-unaudited.md) | a coverage miss lands a mutant in never-executed — exempt from execution and from the audit sample; an ephemeral probe with an oracle test kills it | user decision |
| [mcp-session-disconnect-mid-loop](mcp-session-disconnect-mid-loop.md) | the MCP server dropped mid-session with no call in flight and no diagnosable cause; ask a stated exit reason and an idle-liveness witness | user decision |
