# Issue docs — deferred follow-ups

Tracked deferrals carrying a `Lands:` trigger. On resolution, the
load-bearing rationale is promoted inline to the spec / a test, and the doc
is deleted (git holds history).

| slug | summary | Lands |
|------|---------|-------|
| [attest-has-no-replace-or-withdraw](attest-has-no-replace-or-withdraw.md) | `attest` refuses a second attestation of the same survivor and nothing withdraws one; a narrowed equivalence reason has no home on the record (the ephemeral face has `--reattest`, the findings face does not) | cross-tool train chunk 256 |
| [engine-run-scoped-oracle-value](engine-run-scoped-oracle-value.md) | fourteen engine spawn signatures thread a run-constant bounds value beside env/flags/brackets/namespaces; one engine-side run value would collapse them | cross-tool train chunk 214 |
| [ephemeral-empty-replacement-survives-as-baseline](ephemeral-empty-replacement-survives-as-baseline.md) | an ephemeral batch edit with an empty `new_string` is accepted and reported `SURVIVED` under a file the edit never touched; the same deletion spelled as a comment line kills — a false survivor | a reproduction on a committed tree (the reported tree is on no branch; a same-shape fixture is killed by both spellings) |
| [ephemeral-attestation-lifecycle](ephemeral-attestation-lifecycle.md) | attestation rows have no prune/retarget lifecycle; commit+dirty stamp is the only staleness signal; a pre-commit row names a commit lacking the file | with the next change to prune or retarget record surfaces |
| [ephemeral-probe-writes-under-the-callers-tree](ephemeral-probe-writes-under-the-callers-tree.md) | a probed test's relative writes land in the caller's tree (the overlay runs the test binary in the real package directory); only rapid's failfile writer is disabled — route probes through the scratch channel or narrow REQ-mut-overlay's run-time purity | cross-tool train chunk 187 |
| [explicit-testdata-package-selection](explicit-testdata-package-selection.md) | an explicit fixture directory/import selector cannot load a package excluded by ./...; package-local ephemeral success does not establish campaign freshness | cross-tool train chunk 188 |
| [external-oracle-for-tagged-and-subprocess-targets](external-oracle-for-tagged-and-subprocess-targets.md) | a js/wasm build leg and subprocess-built targets are outside campaign reach in both directions: no oracle can run the browser suite, and a subprocess build compiles the disk, not the mutant; an external-oracle mode or a materialized mutation closes it | user decision — the external-oracle mode (the coverage-bound interim landed at chunk 163: the excluded leg is stated on both faces and in the document) |
| [face-test-fixture-duplicated](face-test-fixture-duplicated.md) | the two faces' tests build one module fixture and one evidence helper twenty-three times | cross-tool train chunk 219 |
| [findings-doc-demotion-unstated](findings-doc-demotion-unstated.md) | a run that demotes a standing repo record into the overlay says nothing; the clause states promotion only | user decision |
| [findings-row-projections-one-shape](findings-row-projections-one-shape.md) | the findings faces project the one inspection walk's rows three ways, differing in the delta's representation alone; one wire shape with a summary projection | cross-tool train chunk 220 |
| [mcp-liveness-cancellation-witness](mcp-liveness-cancellation-witness.md) | keepalive config pinned but the ping-failure cancellation propagation has no witness over the SDK transport seam; the idle-session liveness is unwitnessed too; the exit line loses a session error the SDK discards on cancellation | transport-seam fault injection lands in the mcpserver harness, the idle arm and the last-failure capture beside it |
| [mcp-render-bound-discards-the-response](mcp-render-bound-discards-the-response.md) | a render-bound expiry returns an error and the protocol layer discards the committed run's typed response; the measurement boundary banks instead | cross-tool train chunk 233 |
| [mcp-residue-sheds-unwitnessed](mcp-residue-sheds-unwitnessed.md) | the structured face's residue-shed append has no fixture producing a residue | cross-tool train chunk 219 |
| [mcp-run-observability](mcp-run-observability.md) | Stash's long campaign progressed in the local store without visible progress or a recoverable final report; token-dependent streaming and CLI-only preflight need an actionable caller workflow | cross-tool train chunk 233 |
| [mcp-sheds-and-carries-as-prose](mcp-sheds-and-carries-as-prose.md) | the MCP run response carries sheds and carries as prose sentences where contradictions are rows | user decision |
| [narrowing-exemption-and-freshness-clauses-judged-apart](narrowing-exemption-and-freshness-clauses-judged-apart.md) | a consumer window priced "0 narrowed, N full" on a sharply-covered pure function; the run surface does not say which batch's coverage verdict was unsound or whether a freshness clause withheld the exemption — reach, determinism, and reuse should be judged apart and the ground named | cross-tool train chunk 254 |
| [never-executed-exemption-unaudited](never-executed-exemption-unaudited.md) | a coverage miss lands a mutant in never-executed — exempt from execution and from the audit sample; an ephemeral probe with an oracle test kills it | a reproduction on a gomutant carrying the none-reaching arm (44abf11), with the decision line and the record's measured/served status |
| [own-face-gate-suite-decomposition](own-face-gate-suite-decomposition.md) | chunk-137's narrowing measured weak on gomutant's e2e-heavy face (covering ≈ suite); own-face gates stay on ephemeral probes; suite decomposition is the fork's unchartered (a) half | user decision — deferred 2026-09-07 (the ephemeral gate carries chunk work; decomposition pays only for a full-face campaign) |
| [plan-cost-is-candidate-materialization](plan-cost-is-candidate-materialization.md) | a warm --plan spends 64% of its CPU rendering every candidate (go/format + imports per candidate) though it executes nothing; observation is a small share after the one-view-set fold | with the next change set touching candidate materialization |
| [probe-seam-tuple](probe-seam-tuple.md) | the campaign baseline probe seam returns six values the engine already holds in one result; collapse to the result | cross-tool train chunk 214 |
| [process-identity-reads-have-no-discharge-channel](process-identity-reads-have-no-discharge-channel.md) | gmdb's lock liveness reads /proc/<pid>/stat, so every consumer record is machine-local on a volatile input that is non-behavioral by construction; neither bracket-path nor vouch can declare it | user decision |
| [purity-assertion-run-surface](purity-assertion-run-surface.md) | gofresh's whole-run purity assertion has no gomutant run surface; an interface-invoke over-approximation downgraded a pure function on "reaches net.Dial" and the reason names no invoke site | cross-tool train chunk 254 (the reason half at gofresh 255) |
| [run-faces-assemble-rows-twice](run-faces-assemble-rows-twice.md) | both run faces cut the delta and consult the per-row layer on their own after the final merge | cross-tool train chunk 220 |
| [run-seams-snapshotted-per-run](run-seams-snapshotted-per-run.md) | the run's seams are package state read from its goroutines; a per-run snapshot would make the swap race unrepresentable | cross-tool train chunk 245 |
| [seam-swap-helper-for-tests](seam-swap-helper-for-tests.md) | some sixty hand-written seam save/restore pairs across the root and both faces, seven restoring to a literal | cross-tool train chunk 219 |
| [semantic-closure-in-the-carry-gate](semantic-closure-in-the-carry-gate.md) | a subject-scoped, comment-insensitive closure identity would dominate both poles of the carry gate; the package-scoped one shed on sibling edits (refuted in chunk 130) | cross-tool train chunk 175 (a design chunk opening with the user) |
| [serve-path-posture-keying](serve-path-posture-keying.md) | the run's serve path hands the judgment views keyed by the current oracle's posture where the judgment keys by the recorded one; attribution text only | the next change to the run's serve-path view keying |
| [store-report-path-walk](store-report-path-walk.md) | the findings report path re-walks every record's portable line per call, and the store keeps two per-symbol memos of one entry | cross-tool train chunk 218 |
| [suite-shared-fixture-bracket-flake](suite-shared-fixture-bracket-flake.md) | one full-suite parallel run moved the shared fixturemod/lib observation bracket (writer unidentified; refusal-attribution instrumentation landed in gofresh) | when the instrumented refusal names the writer (next occurrence under a gofresh bump past v0.92.0) |
| [symbol-grammar-package-set-resolution](symbol-grammar-package-set-resolution.md) | symbolPackage's dotted-path guess has one verdict-bearing edge (dotted sibling dirs grant process-attestability across packages); resolve against the loaded package set | when a dotted non-vN package element enters a consumed corpus, or with the next packageProcessAttestable change |
| [unstable-test-quarantine](unstable-test-quarantine.md) | one flaky consumer test flipped a kill and flagged every survivor in its package set unstable-oracle; quarantine the test with its flip evidence and a stated cap instead of degrading the oracle | user decision |
| [vmm-finding-admission-refusals](vmm-finding-admission-refusals.md) | the audit-key half closed at the gofresh v0.99.0 bump (nodwarf5 leg clean); VMM's residual file-I/O (intended), testing-receiver (gofresh immutable-after-construction-objects) and dynamic-carrier (gofresh invoke-targets-narrowed-by-operand) refusals are gofresh positions | cross-tool train chunk 193 (the two gofresh docs) |
| [window-bounds-wiring-unwitnessed](window-bounds-wiring-unwitnessed.md) | the run-level derivation of the window bounds from the worker count is executed by every seam-free run test but discriminated by none (no fixture reaches the floor or the budget) | cross-tool train chunk 219 |
| [windows-process-fact-arms-unexecuted](windows-process-fact-arms-unexecuted.md) | windows process facts (killed flag, job kills, timeout attribution) are compile-checked only; two attribution defects survived to review state walks | when a windows runner enters the CI matrix |
| [freshness-proofs-stall-on-large-record-sets](freshness-proofs-stall-on-large-record-sets.md) | a delta `run` over a ~300-record document spends its whole budget in freshness proofs (union over up to 1608 subjects, ~8 GiB) and never measures | cross-tool train chunk 257 |
| [mcpserver-witnesses-unbound](mcpserver-witnesses-unbound.md) | eighteen MCP server tests pin served behaviour no requirement binds | cross-tool train chunk 219 |
| [knob-terse-clause-per-face](knob-terse-clause-per-face.md) | one knob line serves both faces, so a clause naming the other face reaches each face carrying the other's prose | the bump behind gofresh chunk 266 |
| [root-coordinate-one-spelling](root-coordinate-one-spelling.md) | root-side sites still resolve paths with their own Abs/EvalSymlinks pairs beside the load's one coordinate | cross-tool train chunk 220 |
| [enumeration-lag-gate-unstated](enumeration-lag-gate-unstated.md) | the derived-enumeration lag gate and its effective-configuration source have no clause; three witnesses bind to nothing | cross-tool train chunk 209 |
| [explain-has-no-cli-face](explain-has-no-cli-face.md) | `explain` — why a record is machine-local, what each survivor wants — is served over MCP alone; from a shell or a CI log the only route to the answer is the overlay JSON by hand | cross-tool train chunk 258 |
| [evidence-fault-rendered-as-dirty-provenance](evidence-fault-rendered-as-dirty-provenance.md) | the evidence walk's fail-closed arms stamp a finding dirty on a non-staged run; on a clean tree the portable line reads "dirty worktree provenance" and the true fault — which subject, which manifest — is not carried | cross-tool train chunk 258 |
