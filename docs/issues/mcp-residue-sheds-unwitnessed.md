# The structured face's residue sheds have no witness

The final merge's residue sheds — dispositions the merge strips that no
incremental commit reported — reach the run response through one
append in the structured face's run tool (and, on an error exit after
the write, the error text through the same list). Neutering that
append survives the whole server package: no fixture produces a
non-empty residue, so the wiring is unpinned on that face; the root
package pins the ledger's own residue rule. A fixture whose final merge
strips an attestation no commit reported (a record the run serves
whole while its attested site is gone, or a whole-tree reconcile over
an attested record) would pin the append on both faces.

Lands: cross-tool train chunk 219 (the test surface — the face-scenario
table pins the faces decide alike).
